/*
let upfile = document.querySelector("#file");
let labelBtn = document.querySelector("#student-date__label-btn");
let labelText = document.querySelector('#student-date__label-btn p')


labelBtn.addEventListener('click', () => {
    fileBoxChange(upfile, labelText);
    
})


fileBoxChange = function(fileBox,textBox){
    fileBox.addEventListener('change', function(){
       
    
        let valuesTexts = []
        for(let index = 0; index < fileBox.value.length; index ++){
            if(fileBox.value[index] == '\\'){
                
    
                valuesTexts.push(fileBox.value.slice(index + 1))
                
            }
        }

        
        textBox.textContent = valuesTexts[valuesTexts.length-1]
    
        
    })
    
    

}
*/

    
























